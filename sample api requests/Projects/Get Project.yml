info:
  name: Get Project
  type: http
http:
  method: GET
  url: "{{baseUrl}}/v1/projects/{{projectId}}"
  headers:
    - name: X-User-ID
      value: "{{userId}}"
runtime:
  scripts:
    - type: tests
      code: |-
        test("should return 200", function() {
          expect(res.status).to.equal(200);
        });