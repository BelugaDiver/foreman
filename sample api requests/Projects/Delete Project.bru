meta {
  name: Delete Project
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/v1/projects/{{projectId}}
  body: none
  auth: none
}

headers {
  X-User-ID: {{userId}}
}

tests {
  test("should return 204", function() {
    expect(res.status).to.equal(204);
  });
}
