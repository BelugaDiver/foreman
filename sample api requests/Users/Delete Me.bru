meta {
  name: Delete Me
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/v1/users/me
  body: none
  auth: none
}

headers {
  X-User-ID: {{userId}}
}

tests {
  test("should return 204", function() {
    expect(res.status).to.equal(204);
  });
}
