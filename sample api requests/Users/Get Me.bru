meta {
  name: Get Me
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/v1/users/me
}

headers {
  X-User-ID: {{userId}}
}

tests {
  test("should return 200", function() {
    expect(res.status).to.equal(200);
  });
}
