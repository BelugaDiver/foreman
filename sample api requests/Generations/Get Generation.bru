meta {
  name: Get Generation
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/v1/generations/{{generationId}}
}

headers {
  X-User-ID: {{userId}}
}

tests {
  test("should return 200", function() {
    expect(res.status).to.equal(200);
  });
}
