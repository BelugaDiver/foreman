meta {
  name: Create New Generation
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/v1/projects/{{projectId}}/generations
  body: json
  auth: none
}

headers {
  X-User-ID: {{userId}}
}

body:json {
  {
      "prompt": "simple living room",
      "style_id": null,
      "attempt": 1
    }
}

tests {
  test("should return 200", function() {
    expect(res.status).to.equal(200);
  });
}
