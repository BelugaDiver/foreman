meta {
  name: Delete Image
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/v1/images/{{imageId}}
}

headers {
  X-User-ID: {{userId}}
}

tests {
  test("should return 204", function() {
    expect(res.status).to.equal(204);
  });
}
